# Core UI and data
streamlit>=1.42.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=6.1.0

# Engineering / thermodynamics (vendored engine depends on these)
CoolProp>=6.4.0
fluids>=1.0.0

# Reports & export
openpyxl>=3.1.0          # Excel line-list export
python-docx>=1.0.0       # Word hydraulic report
matplotlib>=3.7.0        # report charts (pure Python — no headless browser needed)
